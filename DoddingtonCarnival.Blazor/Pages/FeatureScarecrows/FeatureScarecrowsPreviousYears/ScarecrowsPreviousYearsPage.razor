@page "/scarecrows/previous"

@using DoddingtonCarnival.Blazor.Services
@using DoddingtonCarnival.Blazor.Models

@inject ScarecrowsService scarecrowsService
@inject NavigationManager NavManager

@inject IJSRuntime JS

<PageTitle>Previous Scarecrows - Doddington Carnival</PageTitle>

<title>Doddington Scarecrows - Previous Entries</title>

<div class="carnivalpage pages paralax carnivalpage-darkblue">
    <div class="container">
        <header>
            <h4 class="line-divider" @onclick="NavigateToParent">Scarecrows</h4>
            <h1>Previous Entries</h1>
        </header>

        <SharedMasonryImages baseService=scarecrowsService />
    </div>
</div>

@code {
    protected override async Task OnInitializedAsync()
    {
    }

    private void NavigateToParent()
    {
        NavManager.NavigateTo("/scarecrows");
    }
}